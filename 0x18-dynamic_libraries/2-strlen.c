#include "main.h"

/**
 * _strlen - entry to prohram to find string length
 * @s: character s
 * Return: i
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; ++i)
	{
		;
	}
	return (i);
}
