#include "main.h"

/**
 * _abs - Computes absolute value
 * print
 * @t: input
 * Return: value for negative
 */

int _abs(int t)
{
	if (t > 0)
		return (t);
	else if (t == 0)
		return (t);
	else
		return (-t);
}
